# Backend Service Configuration
PORT=3000

# Database Configuration
DB_HOST=db
DB_PORT=5432
DB_NAME=taskdb
DB_USER=taskuser
DB_PASSWORD=taskpass

# Note: Copy this file to .env and modify values if needed
# docker-compose.yml will use these values with defaults if .env is not present
